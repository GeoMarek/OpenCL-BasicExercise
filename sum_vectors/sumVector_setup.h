#include "initial_setup.h"

/*
* Adding two vectors and save result in third
* @param hostInput1 - first input vector
* @param hostInput2 - second input vector
* @param hostOutput - it will be sum of input vectors
* @param size - size of arg vectors
*/
void sum_vector(float* hostInput1, float* hostInput2, float* hostOutput, const uint32_t size);